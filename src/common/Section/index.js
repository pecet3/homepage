import { Wrapper, Title } from "./styled";


export const Section = ({title}) => {
    return (
        <>
            <Wrapper>
                <Title>{title}</Title>
            </Wrapper>
        </>
    )
};